Bucket 1
> redo dealer into a class that is not a subclass of discord.client but rather a class which has the client as a member
> refactor as much of mparse into code that works in dealer.py
> create bets class
> create bets manager class (can be within same file as bets class)

Bucket 2
> create spin command
> create send money command
> create wager (a bet between 2 people only)

Bucket 3
> extrapolate all string constants into single file
> switch from json to SQL or someother better solution



